import 'package:flutter/material.dart';

class CoinAmount extends StatelessWidget {
  const CoinAmount({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 50,
      width: 100,
    );
  }
}
